Algoritmo "teste_entrada"

var

x, y, z, a, b, c, d: real


Inicio


         x <- 2.0
         y <- 3.0
         x <- -7.0
         
           a <- Exp(x, y)
           b <- RaizQ(y)
           c <- Pi
           d <- Abs(z)
           
           
             escreva(a)
             escreval(b)
             escreval(c)
             escreval(d)

Fimalgoritmo